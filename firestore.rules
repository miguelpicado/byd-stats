rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // DEVELOPMENT MODE: Allow all reads and writes
    // WARNING: Revert this before going to production
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
